player_health = 100

def take_damage(amount):
    global player_health
    player_health -= amount
    if player_health <= 0:
        print("Game Over")
pygame.draw.rect(screen, (255,0,0), (20, 20, player_health * 2, 20))
